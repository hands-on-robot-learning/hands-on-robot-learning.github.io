---
title: Assignments
description: Milestones from hardware + data to VLA demos.
---

import { parseYaml } from '../../../lib/yaml';
import assignmentsYaml from '../../../data/assignments.yaml?raw';

export const assignmentsData = parseYaml(assignmentsYaml);
export const assignments = assignmentsData.assignments ?? [];
export const participation = assignmentsData.participation;

export const linkById = {
	a1: './a1-build-calibrate-teleop/',
	a2: './a2-handcrafted-cube-picking/',
	a3: './a3-lerobot-imitation-challenge/',
	a4: './a4-vla-smolvla-demo/',
};

export const tagById = {
	a1: ['hardware', 'teleop', 'safety', 'calibration'],
	a2: ['perception', 'baselines', 'evaluation'],
	a3: ['data', 'imitation learning', 'LeRobot', 'evaluation'],
	a4: ['VLA', 'demo', 'showcase', 'failure analysis'],
};

export const mainDeliverableById = {
	a1: 'Safe assembled robot, calibration/config files, and teleoperation evidence.',
	a2: 'Working handcrafted cube-picking baseline with videos, metrics, and failure taxonomy.',
	a3: 'Real-robot dataset, trained policy checkpoint, and official benchmark evaluation.',
	a4: 'VLA-style final demo with evaluation evidence and failure analysis.',
};

export const progression = [
	{ id: 'a1', label: 'A1', title: 'Build / Teleop' },
	{ id: 'a2', label: 'A2', title: 'Baseline' },
	{ id: 'a3', label: 'A3', title: 'Imitation Learning' },
	{ id: 'a4', label: 'A4', title: 'VLA Demo' },
];

This first offering has **four shared assignments** for all students, moving from hardware bring-up to learned policies and a final physical demo.

## Learning progression

<section className="hol-assignment-progression" aria-label="Assignment learning progression">
	{progression.map((step, index) => (
		<article>
			<span>{step.label}</span>
			<strong>{step.title}</strong>
			{index < progression.length - 1 ? <small aria-hidden="true">→</small> : null}
		</article>
	))}
</section>

## Assignments at a glance

<div className="hol-assignment-module-grid">
	{assignments.map((a) => (
		<article className="hol-assignment-module-card">
			<div className="hol-assignment-module-head">
				<span>A{String(a.id).replace('a', '')}</span>
				<strong>{a.weight}%</strong>
			</div>
			<h3>{a.title}</h3>
			<dl className="hol-fact-row">
				<div>
					<dt>Release</dt>
					<dd>Week {a.release_week}</dd>
				</div>
				<div>
					<dt>Due</dt>
					<dd>Week {a.due_week}</dd>
				</div>
			</dl>
			<div className="hol-assignment-main-deliverable">
				<span>Main deliverable</span>
				<p>{mainDeliverableById[a.id] ?? a.deliverables?.[0]}</p>
			</div>
			<p>{a.summary}</p>
			<div className="hol-assignment-tags">
				{(tagById[a.id] ?? []).map((tag) => (
					<span>{tag}</span>
				))}
			</div>
			<a className="hol-assignment-card-link" href={linkById[a.id] ?? './'}>Open full assignment page</a>
		</article>
	))}
</div>

<section className="hol-assignment-showcase-callout">
	<div>
		<p className="hol-section-label">Final showcase</p>
		<h2>Assignments culminate in an evidence-backed robot demo.</h2>
		<p>{'A4 is designed as a public-facing final demo, but the emphasis remains technical: clear task definition, reproducible setup, honest videos, quantitative results where possible, and thoughtful failure analysis.'}</p>
	</div>
</section>

## Participation

{participation.examples?.length ? (
	<ul>
		{participation.examples.map((ex) => (
			<li>{ex}</li>
		))}
	</ul>
) : null}

## Leaderboard philosophy and awards

Leaderboards are used as **motivation and feedback**, not gatekeeping. Grades reward careful engineering: dataset quality,
reproducible evaluation, and honest reporting.

<section className="hol-info-panel" aria-label="Assignment 3 leaderboard details">
	<dl className="hol-fact-row">
		<div>
			<dt>Main metric</dt>
			<dd>{assignments.find((a) => a.id === 'a3')?.leaderboard?.main_metric}</dd>
		</div>
		<div>
			<dt>Tiebreaker</dt>
			<dd>{assignments.find((a) => a.id === 'a3')?.leaderboard?.tiebreaker}</dd>
		</div>
	</dl>
	{assignments.find((a) => a.id === 'a3')?.leaderboard?.awards?.length ? (
		<>
			<h3>Awards</h3>
			<ul>
				{assignments
					.find((a) => a.id === 'a3')
					?.leaderboard?.awards.map((award) => <li>{award}</li>)}
			</ul>
		</>
	) : null}
</section>
